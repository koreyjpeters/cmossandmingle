
<section class="cta gradient">
  <div class="wrap">
    <h2>Bring the Moss Home</h2>
    <a class="btn primary" href="{{ collections.all.products.first.url | default: '/collections/all' }}">Catch the Vibe</a>
  </div>
</section>

{% schema %}
{ "name":"Final CTA","settings":[] }
{% endschema %}
